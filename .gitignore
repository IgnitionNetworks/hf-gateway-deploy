# Active config — copy from starter_config.jsonc and fill in your values
config/config.jsonc

# Runtime data and logs — auto-created by the containers
data/*.db
!data/.gitkeep
logs/*.log
!logs/.gitkeep

# Environment file — copy from .env.example
.env
